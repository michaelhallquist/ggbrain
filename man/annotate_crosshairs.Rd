% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_crosshairs.R
\name{annotate_crosshairs}
\alias{annotate_crosshairs}
\title{Annotate crosshairs at user-specified world (xyz) coordinates}
\usage{
annotate_crosshairs(
  xyz,
  color = "white",
  linewidth = 0.4,
  alpha = 0.7,
  linetype = "dashed",
  tol = 1,
  ...
)
}
\arguments{
\item{xyz}{A numeric matrix or data.frame with columns \code{x}, \code{y}, and \code{z}
giving world-space coordinates (in mm) at which to draw crosshairs.}

\item{color}{Line color for the crosshairs. Default: "white".}

\item{linewidth}{Line width passed to \code{ggplot2::annotate}. Default: 0.4.}

\item{alpha}{Line alpha. Default: 0.7.}

\item{linetype}{Line type (e.g., "dashed"). Default: "dashed".}

\item{tol}{Tolerance (in mm) for matching a coordinate to a displayed slice along its
slicing axis. Default: 1.}

\item{...}{Additional arguments passed to \code{ggplot2::annotate} for the crosshair segments.}
}
\value{
A \code{ggb} object with action "add_annotations"
}
\description{
Annotate crosshairs at user-specified world (xyz) coordinates
}
\details{
Crosshairs are drawn on slices whose slicing axis (e.g., z for axial) falls within
\code{tol} mm of the requested coordinate. If the coordinate on the slicing axis is \code{NA},
the crosshair is shown on all slices along that axis where the remaining two axes match.
For example, \verb{x=0, y=0, z=NA} will show a crosshair on every axial slice at x/y=0 (within
the nearest in-plane voxel). Coordinates are provided in world space to match the
\code{slices()} API.
}
\examples{
\dontrun{
# Show crosshairs on any axial slice at x/y = 0
ggbrain() +
  images(c(underlay = underlay_2mm)) +
  slices(c("z = 20", "z = 40")) +
  geom_brain("underlay") +
  annotate_crosshairs(data.frame(x = 0, y = 0, z = NA), linewidth = 0.6)
}

}
